namespace Test2025101804
{
    //    Day 5：Lambda 表达式与委托入门
    //目标：理解匿名方法、Action<T>、Predicate<T> 的用途。
    //任务内容
    //用 FindAll(Predicate<Book>) 实现筛选；
    //用 ForEach(Action<Book>) 输出结果；
    //体会 foreach → FindAll + ForEach 的写法转变。
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");
        }
    }
}
